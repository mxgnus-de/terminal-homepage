import type { NextPage } from 'next';
import Terminal from '../components/Terminal/Terminal';

const Home: NextPage = () => {
   return (
      <>
         <Terminal />
      </>
   );
};

export default Home;
